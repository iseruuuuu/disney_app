import 'package:flutter/material.dart';

class EmptyTimeLineScreen extends StatelessWidget {
  const EmptyTimeLineScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
